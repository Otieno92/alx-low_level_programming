#include "main.h"

/**
 * print_rev - print a string in reverse followed by a new line
 * @s: variable
 * return: void
 */
void print_rev(char *s)
{
	int y = 0;
	int z;

	while (*s != '\0')
	{
		y++;
		s++
	}
	s--;
	for (y = z; l > 0; z--)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
